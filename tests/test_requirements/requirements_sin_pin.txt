# Formas que el resolver todavía NO sabe escanear. Ninguna debe romperlo: todas
# tienen que volver con skip_reason. Ver "Limitaciones honestas" en
# docs/escaneo_dependencias.md.
-r requirements_1vuln.txt
--index-url https://pypi.org/simple
django>=2.2                       # sin pin exacto
requests~=2.19.0                  # compatible release
pillow==5.2.*                     # pin con comodín
celery[redis]==5.3.0              # extras: SÍ se escanea
urllib3==1.24.1 ; python_version < "3.10"   # con marker: SÍ se escanea
