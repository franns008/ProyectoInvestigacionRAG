# Haystack 3.0.0 (publicada 2026) es un major que ROMPE la API 2.x sobre la que está
# escrito todo el pipeline: entre otras cosas elimina SentenceTransformersSimilarityRanker
# de haystack.components.rankers, que importa pipeline_ciberseguridad.py. Al estar sin
# pinear, el build empezó a resolver 3.0.0 y falló. Se fija <3 hasta migrar a propósito.
# Verificado: 2.31.0 (la última 2.x) todavía expone ese ranker.
haystack-ai>=2.30,<3
# Las integraciones piden haystack-ai>=2.30/2.28 sin techo, así que conviven con 2.31.
# Se les pone techo de major igual, para no repetir esta rotura desde otro paquete.
ollama-haystack>=6.7,<7
pgvector-haystack>=6.3,<7
datasets>=2.6.1
pypdf
python-docx
markdown-it-py
mdit-plain
nltk
requests
python-dotenv
# Comparación de versiones PEP 440 y parseo de requirements.txt (PEP 508) para el
# escaneo de dependencias (src/pipeline/deps/). Ya venía como dependencia transitiva
# de haystack, pero ahora se importa directo, así que se declara.
packaging

# marker-pdf NO va acá: lo instala condicionalmente Dockerfile.pipelines según el
# build-arg INSTALL_MARKER (default false → imagen slim, ~3 GB menos). Sólo hace falta
# para convertir PDFs nuevos a Markdown. Ver docs/imagen_pipelines.md.
